<div class="row">
  <div class="col-lg-6">
  <%= link_to t('back'), clients_path, class: 'btn btn-primary btn-back' %>
  <%= link_to t('show'), @client, class: 'btn btn-primary btn-back' %>
  <h1><%= t('editing_client') %></h1>
  </div>
</div>

<%= render 'form', client: @client %>
