<div class="row">
  <div class="col-lg-6">
  <%= link_to t('back'), cities_path, class: 'btn btn-primary btn-back' %>
  <h1><%= t('editing_city') %></h1>
  </div>
</div>

<%= render 'form', city: @city %>
