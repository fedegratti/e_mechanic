<div class="row">
  <div class="col-lg-6">
  <%= link_to t('back'), cities_path, class: 'btn btn-primary btn-back new__back-button' %>
  <h1 class="new__title"><%= t('new_city') %></h1>
  </div>
</div>

<%= render 'form', city: @city %>
