<%= form_for(manual, html: { class: "form-horizontal", role: "form"}) do |f| %>
  <% if manual.errors.any? %>
    <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
    <h2><%= pluralize(manual.errors.count, "error") %> prohibited this manual from being saved:</h2>

    <ul>
      <% manual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <div class="col-lg-6">
    <div class="well bs-component">

      <div class="form-group">
        <%= f.label t(:car), class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.select :car_id, @cars, {}, { class: "form-control", required: true } %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :operation_number, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.text_field :operation_number, class:'form-control', required: true %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <%= f.submit class: "btn btn-primary" %>
        </div>
      </div>

    </div>
  </div>
<% end %>
