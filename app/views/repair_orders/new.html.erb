<div class="row">
  <div class="col-lg-6">
  <%= link_to t('back'), repair_orders_path, class: 'btn btn-primary btn-back new__back-button' %>
  <h1 class="new__title"><%= t('new_repair_order') %></h1>
  </div>
</div>

<%= render 'form', repair_order: @repair_order %>
